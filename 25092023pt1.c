/*The pre-increment operator (++x) increments the value of the variable before using 
it in an expression. It first adds one to the variable and then returns the updated value*/
#include <stdio.h>
void main(){
    int i =10;
    printf("I=%d\n", ++i);
    printf("I=%d\n",i);
}