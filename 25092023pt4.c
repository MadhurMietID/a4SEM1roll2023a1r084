/*the post-increment operator (x++) increments the value of the variable after using it in an
 expression. It returns the original value of the variable and then adds one to it*/
 void main(){
    int i =10;
    printf("I=%d\n", i++);
    printf("I=%d\n",i);
}